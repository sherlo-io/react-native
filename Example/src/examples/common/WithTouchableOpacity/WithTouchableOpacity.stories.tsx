import { storiesOf } from '@storybook/react-native'
import * as React from 'react'

import WithTouchableOpacity from './WithTouchableOpacity'

storiesOf('WithTouchableOpacity', module).add('default', () => (
  <WithTouchableOpacity />
))
