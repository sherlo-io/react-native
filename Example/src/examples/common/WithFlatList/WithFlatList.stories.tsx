import { storiesOf } from '@storybook/react-native'
import * as React from 'react'

import WithFlatList from './WithFlatList'

storiesOf('WithFlatList', module).add('default', () => <WithFlatList />)
