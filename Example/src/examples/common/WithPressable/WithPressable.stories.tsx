import { storiesOf } from '@storybook/react-native'
import * as React from 'react'

import WithPressable from './WithPressable'

storiesOf('WithPressable', module).add('default', () => <WithPressable />)
