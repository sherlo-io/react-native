import { storiesOf } from '@storybook/react-native'
import * as React from 'react'

import WithImageBackground from './WithImageBackground'

storiesOf('WithImageBackground', module).add('default', () => (
  <WithImageBackground />
))
