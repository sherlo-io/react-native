import { storiesOf } from '@storybook/react-native'
import * as React from 'react'

import WithStatusBar from './WithStatusBar'

storiesOf('WithStatusBar', module).add('default', () => <WithStatusBar />)
