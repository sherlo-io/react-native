import { storiesOf } from '@storybook/react-native'
import * as React from 'react'

import WithTouchableHighlight from './WithTouchableHighlight'

storiesOf('WithTouchableHighlight', module).add('default', () => (
  <WithTouchableHighlight />
))
