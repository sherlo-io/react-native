import { storiesOf } from '@storybook/react-native'
import * as React from 'react'

import WithSectionList from './WithSectionList'

storiesOf('WithSectionList', module).add('default', () => <WithSectionList />)
